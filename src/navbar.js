function Navbar() {
  return (
    <header>
      <div>
        <nav>
          <ul>
            <a href="#">Home</a>
            <a href="#">About Us</a>
            <a href="#">Contact</a>
          </ul>
        </nav>
      </div>
    </header>
  );
}

export default Navbar;
